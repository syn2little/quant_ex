# ============================================================
# quant_ex — Python Dependencies
# Tested with Python 3.9 / 3.10 / 3.11
# ============================================================

# ----------------------------------------------------------
# Quantitative Framework
# ----------------------------------------------------------
pyqlib>=0.9.1            # qlib: data loading, backtest, MLflow recorder
mlflow>=2.0.0            # experiment tracking (used by qlib)
setuptools-scm>=8.0.0    # required when loading qlib from cloned source

# ----------------------------------------------------------
# Machine Learning
# ----------------------------------------------------------
lightgbm>=3.3.0          # gradient boosting model

# Optional — required only for --model mlp (neural network with MPS GPU support)
# On Apple M3: pip install torch
# torch>=2.0.0

# ----------------------------------------------------------
# Data & Market Data
# ----------------------------------------------------------
akshare>=1.10.0          # A-share sector classification (东方财富)
pandas>=1.5.0
numpy>=1.23.0
SQLAlchemy>=1.4.0
PyMySQL>=1.0.0

# ----------------------------------------------------------
# Configuration
# ----------------------------------------------------------
PyYAML>=6.0

# ----------------------------------------------------------
# AI Optimizer (optional — required only for --optimize)
# ----------------------------------------------------------
anthropic>=0.20.0        # Claude API client

# ----------------------------------------------------------
# Notifications (optional — required only if notify enabled)
# ----------------------------------------------------------
requests>=2.28.0

# ----------------------------------------------------------
# Utilities
# ----------------------------------------------------------
python-dateutil>=2.8.0
pytest>=9.0.0             # local agent/test validation
tqdm>=4.64.0             # progress bars in factor mining
